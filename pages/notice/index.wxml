<view class="notice_container">
  <view wx:for="{{noticeList}}" wx:key="{{index}}" class="notice_wrap">
    <view class="title">{{item.title}}</view>
    <view class="content">{{item.content}}</view>
  </view>
</view>